# Workspace Notes

- This is a Vite React TypeScript app.
- Keep the Finwise dashboard local-first and usable without a backend.
- Preserve the student-focused Indian finance data and responsive layout.
- Validate changes with `npm run build` and `npm run lint`.
